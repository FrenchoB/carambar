<div class="modal fade" id="randomJokeModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Ta blague Carambar</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <% if (randomJoke) { %>
          <p><strong>Question:</strong> <%= randomJoke.question %></p>
          <p><strong>Réponse:</strong> <%= randomJoke.answer %></p>
        <% } else { %>
          <p class="text-muted">Aucune blague disponible.</p>
        <% } %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
      </div>
    </div>
  </div>
</div>